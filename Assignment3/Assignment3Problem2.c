#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{


  int x = -1;
    printf("%u", x);


    /*
    // 1)
      int x = -1;
        printf("%u", x); //unsigned variables are always non-negative
        printf("%d", x);

    // 2)
      float var = 2.0;
        scanf("%f", var); // you cannot scan for a variable that is already defined
        printf("\n%.1f", var);

    // 3)
      int x = 5;
        printf("&d", x); // the & symbol should be replaced with a %

    */
}
